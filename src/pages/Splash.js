import React, {useContext, useState, useEffect} from 'react';
import {ImageBackground} from 'react-native';

import {Box, Image, Center} from 'native-base';

export default App = ({navigation, route}) => {
  return <Box bgColor={'white'}></Box>;
};
